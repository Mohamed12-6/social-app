'use client'

// import { addPost, getAllPost } from '@/lib/postslice';
// import { TextareaAutosize } from '@mui/material';
// import { useDispatch } from 'react-redux';


export default function Addpost() {
    // const dispatch=useDispatch<any>()

    // function handlePost(e:Event) {
    //     e.preventDefault();
    //     const formData=new FormData();

    //     const body=e.target?.body.value;
    //     const image=e.target?.image.files[0];

    //     formData.append("body",body)
    //     formData.append("image",image)

    //     // console.log(body,image)

    //     dispatch(addPost(formData)).then(() => {
    //         // يمكنك استدعاء getAllPost مرة أخرى بعد إضافة المشاركة
    //         dispatch(getAllPost());
    //     });

    // }

  return (

    // <form onSubmit={handlePost}>
    //   <TextareaAutosize  name="body" className="textarea-control" minRows={6} maxRows={10} />
    //   <input type="file" className="btn" name="image" id="" />
    //   <button className="btn" type="submit" >Add Post</button>
    // </form>

    <h1></h1>
  );
}
